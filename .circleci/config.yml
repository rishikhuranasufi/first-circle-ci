version: 2.1

jobs:
  first-circle-ci-job:
    machine:
      image: ubuntu-1604:202007-01
    steps:
      - checkout
      - run:
         command: |
            sudo apt update -y

workflows:
  first-workflow:
    jobs:
      - first-circle-ci-job
